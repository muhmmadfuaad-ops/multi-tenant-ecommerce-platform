services:
  db:
    image: postgres:18
    container_name: multi-tenant-ecommerce-platform
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    ports:
      - "5432:5432"
#    volumes:
#      - ./migrations:/docker-entrypoint-initdb.d